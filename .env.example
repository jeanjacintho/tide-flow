# Variáveis de ambiente para o Tide Flow
# Copie este arquivo para .env e ajuste conforme necessário
# IMPORTANTE: O arquivo .env não deve ser versionado (já está no .gitignore)

# ============================================
# API Keys para serviços de IA
# ============================================
# OpenRouter API Key (usado quando llm.provider=openrouter)
# Obtenha em: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Google Gemini API Key (usado quando llm.provider=gemini)
# Obtenha em: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# ============================================
# Configurações de JWT
# ============================================
# IMPORTANTE: Use a mesma secret em todos os serviços (user-service, ai-service)
# Deve ter no mínimo 256 bits (32 caracteres) em produção
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-256-bits

# ============================================
# Email Service (Resend)
# ============================================
# Resend API Key para envio de emails
# Obtenha em: https://resend.com/api-keys
RESEND_API_KEY=

# Modo de desenvolvimento: redireciona todos os emails para EMAIL_DEVMODE
# Valores: true ou false
RESEND_DEVMODE=false

# Email para redirecionamento quando RESEND_DEVMODE=true
EMAIL_DEVMODE=

# ============================================
# Stripe Configuration
# ============================================
# Stripe Secret Key (usado para operações no servidor)
# Obtenha em: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=

# Stripe Publishable Key (usado no frontend)
STRIPE_PUBLISHABLE_KEY=

# Stripe Webhook Secret (para validação de webhooks)
# Obtenha em: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=

# Stripe Enterprise Price ID (opcional, será criado automaticamente se não fornecido)
STRIPE_ENTERPRISE_PRICE_ID=

# URL do frontend (usado para redirecionamentos do Stripe)
FRONTEND_URL=http://localhost:3000

# ============================================
# Frontend URLs (opcional, já configurado no docker-compose)
# ============================================
# URLs para os serviços backend (usado pelos frontends)
# Essas variáveis são usadas pelo docker-compose.yml
# Para desenvolvimento local, configure nos arquivos .env.local dos frontends
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_AI_SERVICE_URL=http://localhost:8082
NEXT_PUBLIC_NOTIFICATION_SERVICE_URL=http://localhost:8081

# ============================================
# Database Configuration (opcional)
# ============================================
# Já configurado no docker-compose.yml, mas pode ser sobrescrito se necessário
# DB_HOST=localhost
# DB_PORT=5432

# ============================================
# RabbitMQ Configuration (opcional)
# ============================================
# Já configurado no docker-compose.yml, mas pode ser sobrescrito se necessário
# RABBITMQ_HOST=localhost

# ============================================
# LLM Provider Configuration (opcional)
# ============================================
# Opções: "ollama", "gemini", ou "openrouter"
# Padrão: "ollama"
# LLM_PROVIDER=ollama
