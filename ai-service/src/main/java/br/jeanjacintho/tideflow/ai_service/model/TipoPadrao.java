package br.jeanjacintho.tideflow.ai_service.model;

public enum TipoPadrao {
    DIARIO,      // Padrão que se repete diariamente
    SEMANAL,     // Padrão que se repete semanalmente (ex: segunda-feira)
    MENSAL,      // Padrão que se repete mensalmente (ex: início do mês)
    SAZONAL      // Padrão que se repete sazonalmente (ex: inverno)
}

